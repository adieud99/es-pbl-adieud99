# 5교시 연습 — Dashboard 조립·Control·Filter·KQL

공통 `products` Dashboard 기준. 실행 2026-09-03.

## (공통·필수) 문제 1 — 6패널을 읽는 순서로 배치

### 배치 기록

- Dashboard 제목: `수업`
- 첫 행 패널: `전체 상품 수` Metric, `카테고리별 상품 수` Bar
- 둘째 행 패널: `브랜드별 상품 수와 평균 가격` Table, `재고 상태 비율` Donut,
  `가격 구간별 상품 수` Bar
- 셋째 행 패널: `월별 상품 등록 수` Line
- 가장 크게 배치한 패널과 이유: 브랜드 Table. 열이 3개(브랜드·상품 수·평균 가격)이고
  행이 40개라 좁으면 브랜드 이름이 잘리고 숫자가 줄바꿈된다. 표는 값을 정확히 읽으라고
  만든 패널이라 잘리면 존재 이유가 없어진다.
- 크기를 늘려 해결한 가독성 문제: **브랜드 이름 열이 잘리는 문제.** 처음 캡처에서 왼쪽이
  잘려 숫자만 보이고 어느 브랜드인지 알 수 없었다. 패널 폭을 늘려 이름·상품 수·평균 가격이
  한 줄에 다 들어오게 했다.
- 제목이 비어 있던 패널과 수정 결과: Metric 패널이 기본값 `Count of records`로 남아
  있었다. `전체 상품 수`로 바꿨다. 차트 이름이 아니라 그 패널이 답하는 내용을 제목으로
  써야 한다.
- 캡처 파일: `evidence/day-04/common-dashboard.png`

배치 원칙은 **위에서 아래로 읽으면 질문이 좁아지게** 잡았다. 첫 행에서 전체 규모를 보고,
가운데에서 그룹별로 나눠 보고, 아래에서 시간 흐름을 본다.

## (공통·필수) 문제 2 — category Options list 추가

`Add → New → Controls → Control → Select a field` 순으로 들어갔다.

- Data View: `products`
- field: `category`
- type: Options list
- label: `카테고리 선택`

### 전후 기록

- 선택한 category: `전자기기`
- 적용 전 Metric: **10,000**
- 적용 후 Metric: **1,250**
- 함께 바뀐 패널 2개:

| 패널 | 적용 전 | 적용 후 |
|---|---|---|
| 브랜드 Table 1위 | 한끼연구소 277 (평균 38,869) | **MobiCore 271 (평균 231,226)** |
| 재고 Donut | true 8,469 / false 1,531 | **true 1,065 / false 185** |
| 가격 구간 Bar 최다 | 5만 미만 3,621 | **20만 이상 723** |

  세 개 이상 바뀌었다. 특히 브랜드 1위와 가격 분포 최다 구간이 **뒤집힌다.** 전체로 보면
  식품 브랜드가 1위고 저가가 제일 많은데, 전자기기만 보면 MobiCore가 1위고 20만원 이상이
  제일 많다. control이 걸렸는지 숫자만 보고도 알 수 있다.

- `Any` 복구 후 Metric: **10,000**
- 정상 여부: **정상**
- 캡처 파일: `evidence/day-04/common-dashboard.png`

## (진단·필수) 문제 3 — Control·Filter·KQL을 구분하고 초기화

한 번에 하나씩만 적용하고 반드시 지운 뒤 다음으로 넘어갔다.

| 방식 | 입력한 조건 | 적용 전 값 | 적용 후 값 | 해제 방법 | 해제 후 값 |
|---|---|---:|---:|---|---:|
| Control | `category` = 전자기기 | 10,000 | **1,250** | 드롭다운에서 `Any` 선택 | 10,000 |
| Filter | `in_stock is false` | 10,000 | **1,531** | filter pill의 `×` 클릭 | 10,000 |
| KQL | `price >= 100000` | 10,000 | **4,090** | 입력줄을 지우고 Enter | 10,000 |

- 세 방식의 사용자가 느끼는 차이:

| | 만든 사람 | 쓰는 사람 | 해제 |
|---|---|---|---|
| **Control** | 대시보드 제작자가 미리 만들어 둠 | 드롭다운에서 고르기만 함. 어떤 값이 있는지 목록으로 보임 | `Any`로 되돌림 |
| **Filter** | 보는 사람이 그때그때 추가 | 알약으로 조건이 눈에 보임. 클릭해서 수정·비활성화 가능 | `×`로 제거 |
| **KQL** | 보는 사람이 직접 타이핑 | 문법을 알아야 함. 오타 나면 결과가 이상해짐 | 지우고 Enter |

  Control은 **반복해서 바꿀 조건**에 쓴다. 소싱 담당자가 매번 카테고리를 바꿔 본다면
  Control이 맞다. Filter는 **한 번 걸어두고 유지할 조건**이나 임시 탐색에 쓴다. KQL은
  복잡한 조건(AND/OR 조합)을 직접 쓸 때 쓴다.

  셋 다 결과는 같은 방식으로 좁혀지지만, 값이 이상할 때 **어디를 봐야 하는지가 다르다.**
  Control은 패널 위쪽 드롭다운, Filter는 알약 줄, KQL은 입력줄이다. 세 군데를 순서대로
  보는 습관이 필요하다(2교시 문제 4에서 이 순서로 진단했다).

- 모든 조건 제거 후 전체값: **10,000**
- `Filter for value` 문구가 없을 때 확인한 filter pill과 변한 패널: 차트 조각을 클릭했을 때
  `Filter for value` 툴팁이 안 뜨는 경우가 있는데, 그래도 클릭하면 상단에 filter pill이
  생긴다. pill이 생겼는지를 보고 판단하면 된다. pill이 생기면 그 조건에 걸린 모든 패널이
  같이 변한다.
- 캡처 파일: 별도 캡처 없음. 각 조건의 값은 `_count`로 대조했다.

## (공통·필수) 문제 4 — 목요일 종료용 저장·재열기

### 저장·복구 기록

- 실제 저장 이름: `수업`
- 저장 시각: 2026-09-03
- 다시 열기 성공 여부: 성공
- 패널 수: 6개
- Control 초기값: `Any`
- KQL/filter 상태: 둘 다 없음
- Metric 값: **10,000**
- 다시 열었을 때 달라진 항목: 없음. 패널 구성·제목·Control 초기값이 그대로 복구됐다.

  다만 **시간 범위는 저장 여부를 확인해야 한다.** Kibana는 저장할 때 `Store time with
  dashboard` 옵션이 꺼져 있으면 시간 범위를 저장하지 않는다. 이 경우 다시 열면 기본값으로
  돌아가 0건이나 9,807건이 나올 수 있다. 절대 범위를 쓸 거면 이 옵션을 켜야 한다.

- 전체 화면 캡처: `evidence/day-04/common-dashboard.png`

## (선택 도전) 문제 5 — 30초 사용성 테스트

실제 테스트는 하지 못했다. 대신 스스로 두 행동을 해보고 걸린 지점을 적는다.

- 상대가 처음 본 패널: 왼쪽 위 Metric. 숫자가 제일 크고 위에 있어서 눈이 먼저 간다.
  의도한 대로다.
- 조건 선택 성공 여부: 성공. Options list가 드롭다운이라 값 목록이 보여서 바로 고를 수 있다.
- 복구 성공 여부: 성공. 다만 `Any`가 "전체"라는 뜻인지 바로 와닿지 않는다.
- 상대가 멈출 만한 지점:
  1. **`Any`의 의미.** 조건을 지우려는 사람이 `×`를 찾다가 못 찾는다. Control에는 지우는
     버튼이 아니라 `Any`로 되돌리는 방식이다.
  2. **브랜드 Table 40행.** 스크롤이 생겨서 아래에 뭐가 더 있는지 모른 채 지나친다.
  3. **막대 높이가 다 같은 category Bar.** 차트가 고장 난 줄 안다. 실제로는 8개가 전부
     1,250건이라 그런 건데, 설명이 없으면 오해한다.
- 수정할 제목·배치·Control label: Control label을 `카테고리 선택`에서
  **`카테고리 (전체 보려면 Any)`** 로 바꾸면 1번이 해결된다. category Bar는 제목을
  `카테고리별 상품 수 (8종 균등)`로 바꿔 오해를 줄일 수 있다.

## 교시 완료 신호

**YELLOW.** 6패널 + Control, 세 조건 전후 비교, 저장·재열기를 다 했다. 다만 최종 값이
교재 기준 20,000이 아니라 10,000이다(1교시 참고). 배치·제목·복구는 정상이다.
